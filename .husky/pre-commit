#!/usr/bin/env sh
# Story TD-4: IDE Sync Pre-commit Auto-Stage
# Automatically syncs agent definitions to IDE command files before each commit

echo "🔄 Running IDE Sync..."

# Run IDE sync with quiet mode for cleaner output
node .aios-core/infrastructure/scripts/ide-sync/index.js sync --quiet

echo "✅ IDE sync complete"

# Run lint-staged for code quality checks
# Note: IDE directories may be gitignored - lint-staged handles this appropriately
npx lint-staged
